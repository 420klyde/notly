import 'package:flutter/material.dart';

const bodyPadding = EdgeInsets.symmetric(horizontal: 13.0);
const noteBodyPadding = EdgeInsets.symmetric(horizontal: 20.0);
const noteFieldPadding = EdgeInsets.only(top: 10.0, left: 15.0, right: 10.0);
